/**
 * 
 */
package net.ruhama.project.services;

import org.springframework.boot.test.context.SpringBootTest;

/**
 * @author ahmedozy
 *
 */
@SpringBootTest
public class UserServiceUnitTest {
	/*
	 * @Autowired private IUserService userService;
	 * 
	 * 
	 * @Test public void addUserTest() { UserDto userDto = new
	 * UserDto("Ahmed Osman Wahbi", "ahmedozy", "password", "password",
	 * Arrays.asList(new AuthorityDto[] {new
	 * AuthorityDto(null,"admin","admin user")}));
	 * userDto.setPhoneNumber(116300001); ObjectResponse<UserDto> response =
	 * userService.addUser(userDto); System.out.println(response); }
	 */
}
